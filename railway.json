{
  "build": {
    "builder": "NIXPACKS"
  },
  "deploy": {
    "startCommand": "gunicorn code:app --bind 0.0.0.0:$PORT --timeout 300",
    "healthcheckPath": "/health",
    "restartPolicyType": "ON_FAILURE"
  }
}