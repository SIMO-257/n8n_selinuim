[phases.setup]
nixPkgs = ["python311", "chromium", "chromedriver"]

[phases.install]
cmds = ["python3 -m pip install -r requirements.txt"]

[start]
cmd = "gunicorn code:app --bind 0.0.0.0:$PORT --timeout 300"